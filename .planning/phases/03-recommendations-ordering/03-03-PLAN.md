---
phase: 03-recommendations-ordering
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - app/order.tsx
autonomous: true
requirements: [ORDR-02]

must_haves:
  truths:
    - "User sees all saved items listed with emoji, name, price, and calories"
    - "User sees total cost and total calories at bottom"
    - "User can tap 'Scan another menu' to restart the demo loop"
  artifacts:
    - path: "app/order.tsx"
      provides: "Order summary screen with saved items and totals"
      min_lines: 80
  key_links:
    - from: "app/order.tsx"
      to: "src/stores/useRecommendationStore.ts"
      via: "useRecommendationStore for saved recommendations"
      pattern: "useRecommendationStore"
    - from: "app/order.tsx"
      to: "app/scan.tsx"
      via: "router.replace for demo restart"
      pattern: "router\\.replace.*scan"
---

<objective>
Build the order summary screen showing saved items with totals and a restart button.

Purpose: Completes the demo loop -- users see their curated picks with total cost and calories, then can restart to scan another menu.
Output: Polished order summary screen matching Stitch mockup.
</objective>

<execution_context>
@/Users/rlocke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rlocke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/03-recommendations-ordering/03-02-SUMMARY.md

<interfaces>
<!-- Contracts from previous plans -->

From src/stores/useRecommendationStore.ts:
```typescript
// toggleSaved and recommendations array with saved boolean
// getSavedItems() returns Recommendation[] where saved === true
```

From src/types/recommendation.ts:
```typescript
export interface Recommendation {
  id: string;
  item: MenuItem;      // has name, price, calories, dietaryTags, description
  matchPercent: number;
  reasoning: string;
  moodId: string;
  saved: boolean;
}
```

From src/types/menu.ts:
```typescript
export interface MenuItem {
  id: string;
  name: string;
  description: string;
  price: number;
  calories?: number;
  dietaryTags?: string[];
  category?: string;
}
```

From src/theme/tokens.ts:
```typescript
export const colors = { bg, card, coral, white, mint, gray, grayDark, ... };
export const spacing = { xs, sm, md, lg, xl, xxl };
export const borderRadius = { sm, md, lg, xl, full };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order summary screen with saved items and totals</name>
  <files>app/order.tsx</files>
  <action>
Replace the placeholder order.tsx with the full order summary screen per DESIGN.md and user decisions.

**Layout (top to bottom):**
1. BackButton (floating, existing component)
2. Header: heading "Your order" in Bold 28-32px white, subtext with count "{N} items" in Regular 14px gray
3. Saved items list (ScrollView, single column)
4. Totals section (sticky or at bottom of scroll)
5. "Scan another menu" restart button

**Empty state (no saved items):**
- Center a message: "No items saved yet" with a fork-and-knife emoji, gray text
- "Go back to picks" coral text link that calls router.back()

**Saved item rows (per DESIGN.md Order Item Row):**
- Background: colors.card (#1A1A1A), borderRadius.lg (16), padding spacing.md
- Horizontal layout: left side has food emoji (from recommendation, or fallback plate emoji) + dish name (Bold 16px white) + calories (Regular 14px gray, e.g., "650 cal")
- Right side: price in Bold 18px white (e.g., "$16.99")
- Gap between rows: spacing.md (16px)
- Get the food emoji from the recommendation object. If the Recommendation type does not have foodEmoji, use a generic plate emoji "🍽️". NOTE: The executor should check what the actual Recommendation/store shape looks like after Plan 01 executes and adapt accordingly.

**Totals section:**
- Separator line: 1px horizontal rule in grayDark color, margin vertical spacing.lg
- "ESTIMATED TOTAL" label: Medium 11-12px gray, uppercase, letter-spacing 0.05em (per DESIGN.md section labels)
- Total cost: Bold 28-32px white, formatted as "$XX.XX"
- Total calories: Regular 16px gray, formatted as "~X,XXX cal"
- Compute totals: sum of all saved item prices and calories (use 0 for missing calories)

**Restart button:**
- Full-width coral pill button (colors.coral bg, white text, borderRadius.full, height 56px)
- Leading camera emoji + "Scan another menu" text in Bold 16px
- On press: clear both stores (useRecommendationStore().clear() and useMenuStore().clear()), then router.replace('/scan') to restart demo loop without back-stack

**Screen background:** colors.bg (#0A0A0A)

**Skip Phase 4 features** (per user decision): No Taste DNA card, no share buttons, no rate button. Clean Phase 3 scope.

Use StyleSheet.create with theme tokens throughout. No hardcoded values.
  </action>
  <verify>
    <automated>cd /Users/rlocke/projects/cravr && npx tsc --noEmit app/order.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Order screen shows saved items with emoji + name + calories + price. Totals section shows summed cost and calories. "Scan another menu" button clears stores and navigates to scan screen. Empty state shows "No items saved yet" with back link.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes on app/order.tsx
- Order screen renders saved items from recommendation store
- Total cost and calories are computed correctly
- "Scan another menu" clears state and navigates to scan
- Empty state renders when no items are saved
</verification>

<success_criteria>
Order summary screen completes the full demo loop. User can see all saved items with totals and restart by scanning another menu. The entire Phase 3 flow (mood -> results -> order) is functional end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-recommendations-ordering/03-03-SUMMARY.md`
</output>
