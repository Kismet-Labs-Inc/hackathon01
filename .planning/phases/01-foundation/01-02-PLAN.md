---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/index.tsx
  - src/components/ui/BackButton.tsx
  - app/scan.tsx
  - app/mood.tsx
  - app/results.tsx
  - app/order.tsx
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02

must_haves:
  truths:
    - "Welcome screen matches Stitch mockup -- fire emoji, Cravr title, tagline, coral CTA, radial glow"
    - "Welcome screen has fade-in animation on logo/tagline and slide-up on CTA button"
    - "Tapping 'Scan a Menu' navigates to the Scan screen"
    - "All placeholder screens have consistent styling with floating back button and Next navigation"
    - "Coral accent #E8734A renders correctly on CTA button and tagline"
  artifacts:
    - path: "app/index.tsx"
      provides: "Polished Welcome screen matching Stitch mockup"
      min_lines: 60
    - path: "src/components/ui/BackButton.tsx"
      provides: "Reusable floating back button component"
      exports: ["BackButton"]
  key_links:
    - from: "app/index.tsx"
      to: "/scan route"
      via: "router.push on CTA press"
      pattern: "router\\.push.*scan"
    - from: "app/index.tsx"
      to: "src/theme/tokens.ts"
      via: "design token imports for colors"
      pattern: "import.*tokens"
    - from: "src/components/ui/BackButton.tsx"
      to: "expo-router"
      via: "router.back() on press"
      pattern: "router\\.back"
---

<objective>
Build the polished Welcome screen matching the Stitch mockup and refine all placeholder screens with consistent Cravr styling.

Purpose: This is the first screen users see and defines the visual identity. A polished Welcome screen with smooth animations proves the design system works end-to-end.
Output: A demo-ready Welcome screen and styled placeholder screens with proper navigation.
</objective>

<execution_context>
@/Users/rlocke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rlocke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

Visual reference -- match this closely:
@ux/1-welcome-stitch.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build polished Welcome screen matching Stitch mockup</name>
  <files>app/index.tsx</files>
  <action>
Replace the stub Welcome screen with a polished implementation matching the Stitch mockup exactly.

Layout (top to bottom, centered horizontally, dark #0A0A0A background):
1. Fire emoji -- large size (~80px), positioned in upper-center area (~30% from top)
2. Warm radial glow behind fire emoji -- use expo-linear-gradient or a radial gradient View. Create a circular warm orange glow (rgba(232, 115, 74, 0.15) fading to transparent) behind the emoji, ~200px diameter. This creates the warm ambient light effect from the mockup.
3. "Cravr" -- bold white text, Plus Jakarta Sans Bold, large (~40px font size), directly below emoji
4. "Scan. Vibe. Devour." -- coral-muted color (#E8734A at ~60% opacity), Plus Jakarta Sans Medium, ~18px, below title
5. Large gap (flex spacer pushing CTA to bottom)
6. "Scan a Menu" CTA button:
   - Full-width (with horizontal padding ~24px)
   - Background: #E8734A coral
   - Text: white, Plus Jakarta Sans SemiBold, ~18px
   - Camera icon (use a simple unicode camera icon or text -- keep it simple for hackathon)
   - Rounded corners (borderRadius: 16 or use rounded-2xl)
   - Height: ~56px
   - onPress: router.push("/scan")
   - Button press effect: scale down slightly on press (use Pressable with style callback, scale to 0.97)
7. "No signup needed -- just point and shoot" -- muted white text (~50% opacity), Plus Jakarta Sans Regular, ~14px, below button, centered
8. Bottom padding for safe area

Animations (per user decision -- subtle fade-in):
- Use react-native-reanimated for smooth native animations
- On mount, fire emoji + "Cravr" + tagline fade in together over ~500ms (opacity 0->1)
- CTA button slides up from below (~20px translate) and fades in, starting ~300ms after logo group, over ~400ms
- "No signup needed" text fades in after button, over ~300ms
- Use useSharedValue + withTiming from reanimated
- Use Animated.View wrappers (from reanimated, NOT from react-native)

Ensure StatusBar is light-content style. No header bar, no navigation chrome.
  </action>
  <verify>
    <automated>cd /Users/rlocke/projects/cravr && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Welcome screen renders with fire emoji, warm radial glow, "Cravr" title in bold white, "Scan. Vibe. Devour." tagline in muted coral, large coral "Scan a Menu" CTA with camera icon, "No signup needed" subtext. Fade-in animation plays on mount. Tapping CTA navigates to Scan screen.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable BackButton and polish placeholder screens</name>
  <files>src/components/ui/BackButton.tsx, app/scan.tsx, app/mood.tsx, app/results.tsx, app/order.tsx</files>
  <action>
Create src/components/ui/BackButton.tsx:
- Absolute positioned in top-left (top: 56 for status bar clearance on iOS, left: 16)
- 44x44 circle with #1A1A1A background at 80% opacity
- White left-pointing chevron (use unicode character at ~24px or simple arrow text -- keep hackathon-simple)
- onPress: router.back() from expo-router
- Accept optional `onPress` prop override (for custom navigation like "restart demo")
- Pressable with opacity feedback on press

Update all 4 placeholder screens (scan, mood, results, order) with consistent styling:

Each screen should have:
- Full dark #0A0A0A background
- BackButton component in top-left (except scan could skip it if Welcome is the only predecessor, but include it for consistency)
- Screen name displayed large and centered: Plus Jakarta Sans Bold, white, ~28px
- Subtle description below in muted white: what this screen will become
  - Scan: "Camera and gallery menu capture"
  - Mood: "Pick your vibe"
  - Results: "AI-matched recommendations"
  - Order: "Your picks summary"
- "Next" button at bottom (same coral style as Welcome CTA but slightly smaller, ~48px height):
  - Scan -> router.push("/mood"), text: "Next: Pick a Mood"
  - Mood -> router.push("/results"), text: "Next: See Results"
  - Results -> router.push("/order"), text: "Next: Order Summary"
  - Order: TWO buttons -- "Restart Demo" (coral, navigates to /scan) and no Next (this is the end)
- Order screen "Restart Demo" uses router.replace("/scan") to clear the stack per user decision (demo restart skips Welcome)

All text uses Plus Jakarta Sans via NativeWind font-jakarta classes or direct fontFamily style.
Use SafeAreaView or appropriate padding to avoid notch/status bar overlap.
  </action>
  <verify>
    <automated>cd /Users/rlocke/projects/cravr && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>BackButton component renders as floating circle with chevron on all inner screens. All 4 placeholder screens show their name, description, and Next/Restart buttons with Cravr styling. Full navigation flow works: Welcome -> Scan -> Mood -> Results -> Order -> Restart to Scan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Welcome screen and navigation on real device</name>
  <files>none</files>
  <action>
Human verifies the complete Welcome screen and navigation flow on a real phone via Expo Go.

What was built: Complete Welcome screen matching Stitch mockup with Cravr design system, fade-in animations, and full navigation flow through all 5 screens with styled placeholders.

How to verify:
1. Run `npx expo start` and open on your phone via Expo Go
2. Welcome screen should appear with:
   - Fire emoji with warm radial glow behind it
   - "Cravr" in bold white, "Scan. Vibe. Devour." in muted coral
   - Subtle fade-in animation on load (~0.5s)
   - Large coral "Scan a Menu" button that slides up from bottom
   - "No signup needed -- just point and shoot" text below
3. Tap "Scan a Menu" -- should navigate to Scan placeholder
4. Tap through all screens: Scan -> Mood -> Results -> Order
5. Each screen should have dark background, floating back button, and Next button
6. On Order screen, tap "Restart Demo" -- should go to Scan (skipping Welcome)
7. Verify Plus Jakarta Sans font is rendering (different from system default)
8. Verify no bottom tab bar anywhere

Resume: Type "approved" or describe any issues.
  </action>
  <verify>
    <automated>cd /Users/rlocke/projects/cravr && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms Welcome screen matches mockup, animations play smoothly, navigation flow works end-to-end, and design system renders correctly on a real device.</done>
</task>

</tasks>

<verification>
1. Welcome screen visually matches the Stitch mockup (fire emoji, radial glow, title, tagline, CTA)
2. Fade-in animation plays on Welcome screen load
3. Full navigation flow works without crashes: Welcome -> Scan -> Mood -> Results -> Order
4. Back navigation works on all inner screens
5. Demo restart from Order goes to Scan, not Welcome
6. Plus Jakarta Sans font renders throughout (not system default)
7. No header bars or bottom tab bars visible
8. All text uses Cravr design colors (white, coral, muted variants)
</verification>

<success_criteria>
- Welcome screen is demo-ready -- matches Stitch mockup closely enough for hackathon
- All 5 screens accessible and navigable
- Animations are smooth (native driver via reanimated)
- Design system proven: dark theme, coral accent, Plus Jakarta Sans all rendering correctly
- Human verification confirms app looks correct on real device
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
